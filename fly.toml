app = "backend-porudzbine-vegnoa"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "3000"

[[services]]
  internal_port = 3000
  protocol = "tcp"

  # 🔹 Ovo dodajemo da omogući automatsko spavanje i buđenje
  auto_stop_machines = true       # zaustavi kad nema saobraćaja
  auto_start_machines = true      # probudi kad neko pošalje zahtev
  min_machines_running = 0        # dozvoli da nema nijedna aktivna mašina

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443
